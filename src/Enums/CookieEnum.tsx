/**
 * This is an enum to prevent misspelling and other errors that can occur with Cookie access.
 */

export enum CookieEnum {
    USERTYPE = 'userType',
    USERNAME = 'userName',
    SESSION  = 'sessionToken',
    USERID   = 'userId',
}