import React from 'react';

interface StudentGradesListProps {

}

export const StudentGradesList: React.FC<StudentGradesListProps> = ({}) => {
    return (
        <div>
            TODO: Render Student Grades
        </div>
    );
};

export default StudentGradesList;